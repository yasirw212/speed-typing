pipeline {
    agent any
    
    stages {
        stage('Pull code from repository'){
            steps {
                sh ('touch app.js')
                sh ('echo hello >> app.js')
            }
        }
    }
}
